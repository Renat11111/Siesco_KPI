# Siesco KPI (Analytical Dashboard)

Профессиональная десктопная система (Go + Wails + React) для анализа эффективности сотрудников через парсинг Excel-отчетов и интеграцию с Bitrix24.

## 🏗 Текущая архитектура (Refactored)

*   **Backend:** Кастомный PocketBase сервер.
    *   `cmd/server/main.go` — Точка входа.
    *   `internal/core/` — Ядро системы (хуки, правила, схемы БД).
    *   `internal/handlers/` — Обработчики API (Рейтинги, Аналитика, Bitrix).
    *   `internal/utils/` — Высокопроизводительные хелперы (Replay Logic).
*   **Frontend:** Wails + React + Vite.
    *   `pocketbase-ui/` — Контейнер десктопного приложения.
    *   `pocketbase-ui/frontend/src/components/` — Динамические чарты и модули управления.

## 🧠 Инженерные принципы и оптимизация

Для обеспечения стабильности при больших объемах данных (100k+ задач) внедрены:
*   **Memory Efficiency:** Использование `StreamRanking` (обработка строк БД через итераторы без загрузки всего массива в RAM).
*   **Data Precision:** Принудительное использование `json.Number` во всех парсерах для исключения потери точности ID и часов.
*   **Realtime Signaling:** Глобальная синхронизация рейтингов через коллекцию `ranking_updates` (интерфейс обновляется у всех пользователей мгновенно при загрузке новых данных).
*   **Dynamic Schema:** Полная зависимость UI от `config.json`. Поля задач и статусы синхронизируются с БД при старте.

## 🛠 Статус разработки

> **ВНИМАНИЕ:** Проект находится в фазе активного рефакторинга после выявления ошибок агрегации и визуализации. 
*   [x] Исправлена визуализация графиков динамики (точки, шкалы, локализация).
*   [ ] Оптимизация логики суммирования часов в рейтингах (в процессе).
*   [ ] Внедрение `sync.Pool` для высоконагруженных JSON-обработчиков.

## 🚀 Сборка (Build Rules)

1.  **Backend:** `go build -o build/SiescoBackend.exe ./cmd/server/main.go`
2.  **Frontend:** `cd pocketbase-ui && wails build -o SiescoKPI.exe`
Финальные бинарные файлы всегда находятся в папке `build/` в корне проекта.

## 📄 Лицензия
Private Property of Renat / Siesco.