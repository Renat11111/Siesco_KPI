# Siesco KPI System

Профессиональная система мониторинга ключевых показателей эффективности (KPI) сотрудников, построенная на базе PocketBase и Wails.

## 🚀 Основные возможности

*   **Динамическая конфигурация:** Все статусы задач и поля Excel-отчетов настраиваются через `config.json` без пересборки приложения.
*   **Изоляция сессий:** Поддержка одновременной работы нескольких окон Wails под разными учетными записями без конфликтов.
*   **Умная аналитика:** Месячные и годовые рейтинги с оптимизацией памяти (Streaming SQL), способные обрабатывать миллионы записей.
*   **Real-time обновление:** Интерфейс мгновенно реагирует на изменения в базе данных (статусы, новые задачи).
*   **Безопасность:** Строгая валидация дат, проверка пересечения периодов для отгулов и контроль прав доступа.
*   **Импорт из Excel:** Гибкий парсинг отчетов с автоматическим сопоставлением колонок и проверкой типов данных.

## 🛠 Технологический стек

*   **Backend:** Go (v1.25+), PocketBase (embedded).
*   **Frontend:** React (v18), TypeScript, Vite.
*   **Desktop Shell:** Wails (v2).
*   **Database:** SQLite + SQL Views.

## 📁 Структура проекта

### Бэкенд (Корень)
*   `main.go` — инициализация сервера и маршрутов.
*   `handlers.go` — логика обработки API-запросов.
*   `helpers.go` — утилиты для парсинга и валидации.
*   `config.go` — описание структур данных и константы.
*   `helpers_test.go` — unit-тесты бэкенда.

### Фронтенд (`/pocketbase-ui/frontend`)
*   `src/lib/SettingsContext.tsx` — глобальное управление настройками.
*   `src/hooks/useTaskFilters.ts` — логика фильтрации и итогов.
*   `src/lib/excelUtils.ts` — ядро парсинга Excel.

## ⚙️ Установка и запуск

### 1. Запуск Бэкенда (PocketBase)
```bash
go run . serve
```
Сервер будет доступен по адресу `http://127.0.0.1:8090`.

### 2. Запуск Фронтенда (Режим разработки)
```bash
cd pocketbase-ui
wails dev
```

### 3. Сборка финального приложения
```bash
cd pocketbase-ui
wails build
```
Готовый файл появится в `pocketbase-ui/build/bin/SiescoKPI.exe`.

## 🧪 Тестирование
```bash
go test ./...
```

## 📄 Лицензия
Private Property of Renat / Siesco.
