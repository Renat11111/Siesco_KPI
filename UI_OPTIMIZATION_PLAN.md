# План оптимизации UI проекта Siesco KPI (Обновлено 30.12.2025)

## ✅ Фаза 1: Чистота кода и Унификация — ВЫПОЛНЕНО
*   **Результат:** Создана библиотека общих UI-компонентов в `src/components/ui/`.
*   **Компоненты:** `Card`, `Badge`, `StatusBadge`, `UserBadge`, `MultiSelect`.
*   **Итог:** Код очищен от дублирования стилей, дизайн стал консистентным во всем приложении.

## ✅ Фаза 2: Архитектура (Hooks) — ВЫПОЛНЕНО
*   **Результат:** Вся бизнес-логика вынесена из компонентов в кастомные хуки в `src/hooks/`.
*   **Хуки:** `useBitrixData`, `useTaskList`, `useDailyStats`, `useMonthlyStats`, `useComparisonStats`, `useTaskUpload`.
*   **Итог:** Файлы компонентов сокращены на 40-60%, разделены зоны ответственности (UI отдельно, Logic отдельно).

## 🚀 Фаза 3: Надежность (Тестирование логики) — СЛЕДУЮЩИЙ ШАГ
**Цель:** Гарантировать точность расчетов KPI, так как ошибки в часах критичны.
1.  **Unit-тесты для Math-логики:**
    *   Покрыть тестами функции расчета часов (с учетом `original_time_spent`).
    *   Протестировать логику "воспроизведения истории" (Replay Logic) для рейтингов.
    *   Валидация отгулов (Time Off) — проверка пересечений дат.
2.  **Инструментарий:** Vitest или Jest (настроить минимальное окружение).

## 🛠 Фаза 4: Финальная полировка (Остаточные компоненты) — В ОЧЕРЕДИ
**Цель:** Довести до идеала оставшиеся второстепенные компоненты.
1.  **`LeaveRequests.tsx`**: Вынести логику заявок на отгулы в хук `useLeaveRequests` и унифицировать UI (Card/Badge).
2.  **`NotificationCenter.tsx`**: Рефакторинг логики уведомлений.
3.  **`Auth.tsx` / `ChangePasswordModal.tsx`**: Привести к общему стилю.

---
**Примечание для следующей сессии:** Начинаем с **Фазы 3**. Архитектура готова к внедрению тестов, так как логика теперь изолирована в хуках и хелперах.